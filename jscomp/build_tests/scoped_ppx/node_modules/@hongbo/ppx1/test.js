#!/usr/bin/env node
//@ts-check
var fs = require('fs')

let [_node, _js,  file_in, file_out] = process.argv
// console.log(process.argv)
// debugger;
fs.createReadStream(file_in)
    .pipe(fs.createWriteStream(file_out))

