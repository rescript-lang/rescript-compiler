module A = {
  type props<'x> = {x: 'x}
  let make = ({x, _}: props<_>) => React.string(x)
  let make = {
    let \"Interface$A" = React.component((props: props<_>) => make(props))
    \"Interface$A"
  }
}

module NoProps = {
  type props = {}

  let make = (_: props) => ReactDOM.jsx("div", {})
  let make = {
    let \"Interface$NoProps" = React.component(props => make(props))

    \"Interface$NoProps"
  }
}
