{
  "scripts": {
    "clean": "rescript clean -with-deps",
    "build": "rescript build",
    "watch": "rescript build -w"
  },
  "dependencies": {
    "bs-fetch": "^0.6.2",
    "rescript": "file:.."
  },
  "private": true,
  "devDependencies": {
    "@rescript/react": "^0.10.3"
  }
}
