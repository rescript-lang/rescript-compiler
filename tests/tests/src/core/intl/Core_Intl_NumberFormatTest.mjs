// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Primitive_option from "rescript/lib/es6/Primitive_option.js";
import * as Intl_NumberFormat_Grouping from "rescript/lib/es6/Intl_NumberFormat_Grouping.js";

console.log("---");

console.log("Intl.NumberFormat");

let currencyFormatter = new Intl.NumberFormat(["fr-FR"], {
  currency: "EUR",
  style: "currency"
});

console.log(Intl.NumberFormat.supportedLocalesOf([
  "fr-FR",
  "en-US"
]));

console.log(currencyFormatter.format(123.23));

let roundingFormatter = new Intl.NumberFormat(undefined, {
  roundingIncrement: 500,
  minimumFractionDigits: 2,
  maximumFractionDigits: 2
});

let groupingFormatter1 = new Intl.NumberFormat(undefined, {
  useGrouping: Primitive_option.some(true)
});

let groupingFormatter2 = new Intl.NumberFormat(undefined, {
  useGrouping: "auto"
});

let sigFormatter = new Intl.NumberFormat(undefined, {
  minimumIntegerDigits: 1,
  minimumFractionDigits: 1,
  maximumFractionDigits: 1,
  minimumSignificantDigits: 1,
  maximumSignificantDigits: 1
});

let options = sigFormatter.resolvedOptions();

console.log(options);

console.log(Intl_NumberFormat_Grouping.parseJsValue(options.useGrouping));

export {
  currencyFormatter,
  roundingFormatter,
  groupingFormatter1,
  groupingFormatter2,
  sigFormatter,
  options,
}
/*  Not a pure module */
