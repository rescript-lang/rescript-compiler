// es6 arrow shouldn't be picked up here
if color === Black && (sibling === None || parent === None) {
  ()
}

if color === Black && color !== Red && (sibling === None || parent === None) {
  ()
}

switch color === Black && (sibling === None || parent === None) {
| _ =>  ()
}

switch color === Black && color !== Red && (sibling === None || parent === None) {
| _ =>  ()
}

try color === Black && (sibling === None || parent === None) catch {
| _ =>  ()
}

try color === Black && color === Red && (sibling === None || parent === None) catch {
| _ =>  ()
}

while color === Black && (sibling === None || parent === None) {
  ()
}

while color === Black && color === Red && (sibling === None || parent === None) {
  ()
}

<div
  onClick={event => {
    switch videoContainerRect {
    | Some(videoContainerRect) =>
      // should not be picked up as es6 function
      let newChapter = ({startTime: percent *. duration}: Video.chapter)
      {a, b}->onChange
    | _ => ()
    }
  }}
/>

if inclusions[index] = (uid, url) {
  onChange(inclusions)
}
