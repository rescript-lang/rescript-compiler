{
  "scripts": {
    "build": "gitbook install && gitbook build && cp -r _book/* $BUCKLESCRIPT_DOC",
    "watch": "gitbook install && gitbook serve"
  },
  "devDependencies": {
    "gitbook-cli": "^2.1.3"
  }
}
