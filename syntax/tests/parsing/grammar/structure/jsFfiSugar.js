import setTimeout: (unit => unit, unit) => float

import setTimeout as timeout: (unit => unit, unit) => float

import {
  setTimeout: (unit => unit, unit) => float,
  clearTimeout: float => unit
}

import { random: unit => float } from Math

import { href: string } from window.location

import {
  dirname: string => string
} from "path"
