type t = private 'a
type t = private string
type t = private _
type t = private (int)
type t = private (int) => int
type t = private int => int
type t = private int => int => int
type t = private (int, ~x:string, float) => unit
type t = private string as 'x
type t = private %ext
type t = private %ext("console.log")
type t = private React.element

